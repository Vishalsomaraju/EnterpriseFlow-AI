{
  "name": "invoice-automation",
  "version": "1.0.0",
  "description": "Acme Corp Invoice Automation - runnable baseline target for IBM Bob.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "vitest": "^1.6.0",
    "typescript": "^5.4.5",
    "@types/node": "^20.14.0"
  }
}
